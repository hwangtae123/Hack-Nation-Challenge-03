[pytest]
# Only collect this project's tests. The vendor starter pack ships its own
# starter/src + starter/tests whose "src" package name collides with ours.
testpaths = tests rag/tests
norecursedirs = realdoor-hackathon-starter-pack .git __pycache__ rag/corpus rag/index
